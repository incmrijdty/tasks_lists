<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= task.id %> - Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/main.css" />
  </head>
  <body>
    <%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath:
      activeLinkPath }); %>
    <main class ="layout">
      <aside class="sidebar">
        <h2>Your Tasks</h2>
        <ul class="task_menu">
          <% savedTasks.forEach(task => { %>
          <li><a href="/<%= task.id %>"><%= task.id %></a></li>
          <% }); %>
        </ul>
        <div id="modalOverlay" class="modal-overlay hidden">
          <div class="modal-window">
            <h2>Create New Task</h2>
            <form action="/add" method="POST">
              <input type="text" id="id" placeholder="task id" required />
              <input type="text" content="content" placeholder="Task content" required />
              <div style="margin-top: 1rem;">
                <button class ="button" type="submit" class="button">Create</button>
                <button type="button" class="button close-modal">Cancel</button> 
              </div>
            </form>
          </div>
        </div>
        <button class="create_task_btn" id="openModalBtn">+ Create New task</button>
      </aside>

      <section class="content_area">
        <h1 class="heading"><%= task.id %></h1>
        <form class= "delete_task" action="/<%= task.id %>?_method=DELETE" method="POST"
            onsubmit="return confirm('Are you sure you want to delete this task?');">
            <button class="button" type="submit">Delete the task</button>
        </form>
      </section>
    </main>

    <script>
      document.addEventtaskener('DOMContentLoaded', () => {
        const modal = document.getElementById('modalOverlay');
        const openBtns = document.querySelectorAll(".create_task_btn_dp_dwn");
        const closeBtns = document.querySelectorAll('.close-modal');
        const openBtn = document.getElementById('openModalBtn');

        openBtns.forEach(openBtn => {
          openBtn.addEventtaskener('click', () => {
            modal.classtask.remove('hidden');
          });
        });

        openBtn.addEventtaskener('click', () => {
          modal.classtask.remove("hidden");
        });

        closeBtns.forEach(btn => {
          btn.addEventtaskener('click', () => {
            modal.classtask.add('hidden');
          });
        });

        modal.addEventtaskener('click', (e) => {
          if (e.target === modal) {
            modal.classtask.add('hidden');
          }
        });
      });
    </script>

    <div id="modalOverlay" class="modal-overlay hidden">
      <div class="modal-window">
        <h2>Create New Task</h2>
        <form action="/add" method="POST">
          <input type="text" id="id" placeholder="task id" required />
          <input type="text" content="content" placeholder="Task content" required />
          <div style="margin-top: 1rem;">
            <button class ="button" type="submit" class="button">Create</button>
            <button type="button" class="button close-modal">Cancel</button> 
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
